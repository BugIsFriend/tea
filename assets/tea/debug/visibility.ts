import { Unit } from "../unit";



enum eVisibility { 
    Position,       // 显示位置；
    Ray,            // 显示射线
    BoundingBox,    // 包围盒；
    
}

/**
 * 测试阶段显示，节点，位置，包围盒；动态追踪物体位置
 * 显示指定向量；
 */
export class Visibility extends Unit { 


}