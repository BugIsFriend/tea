# 添加全局变量

## TODO list

   1. debug， 测试模块的组件；参照 zyb 设计思路；
   2. Dialog;
   3. Tip
   4. TipBox

### component:组件模块，可以单独使用，尽量少的依赖

   1. audio-component: 参照 zyb 声音组件；
   2. loader-component: 参照 zyb 加载组件，load(asset, assetType, function, context)；
   3. run-in-editor // 在编辑器实现更新；
